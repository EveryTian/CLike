类C语言源码
     |  lex生成的lexanalyzer（C语言程序）
     |
token序列（token是结构体，存入类型，行号和值）
  |  	|  bison生成的parser（C语言程序）
  |   	| （parser分析时直接用token值计算出结果）
  |   实际语法
  | 
  | （分析时将token值嵌套填入结构体，形成树形结构）
抽象语法树（注意连续定义的函数和结构体需要放入一个大的定义结构体中，以便递归定义）
  |	     |
  |	     |语义分析（C程序，连接变量定义和值，检查变量类型，简化数据结构）
  |	符号表（哈希表实现，变量与函数两个命名空间）
  |	     |
  |	     |处理局部变量
  |	栈帧式活动记录
  |	     |
  |	     |
中间代码（树结构表示）
